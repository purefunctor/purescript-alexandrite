---
source: tests-integration/src/fixtures.rs
assertion_line: 256
expression: ssa_report
---
@export const forward = initialize {
  entry() {
    const later = global(later);
    return later;
  }
}

@export const later = initialize {
  entry() {
    const literal = 42;
    return literal;
  }
}

@export recursive[2] function first($boolean) {
  entry() {
    case$0();
  }
  case$0() {
    const matches = pattern.literal($boolean, true);
    if (matches) {
      match$success();
    } else {
      case$1();
    }
  }
  case$1() {
    const matches$1 = pattern.literal($boolean, false);
    if (matches$1) {
      match$success$1();
    } else {
      case$failure();
    }
  }
  case$failure() {
    throw patternMatchFailure();
  }
  case$join(result) {
    return result;
  }
  match$success() {
    const literal = 1;
    case$join(literal);
  }
  match$success$1() {
    const second = global(second);
    const literal$1 = true;
    const call = source.call(second, literal$1);
    case$join(call);
  }
}

@export recursive[2] function second($boolean) {
  entry() {
    case$0();
  }
  case$0() {
    const matches = pattern.literal($boolean, true);
    if (matches) {
      match$success();
    } else {
      case$1();
    }
  }
  case$1() {
    const matches$1 = pattern.literal($boolean, false);
    if (matches$1) {
      match$success$1();
    } else {
      case$failure();
    }
  }
  case$failure() {
    throw patternMatchFailure();
  }
  case$join(result) {
    return result;
  }
  match$success() {
    const literal = 2;
    case$join(literal);
  }
  match$success$1() {
    const first = global(first);
    const literal$1 = true;
    const call = source.call(first, literal$1);
    case$join(call);
  }
}

@export recursive[3] const recursiveValue = initialize {
  entry() {
    const closure = closure(recursiveValue$initialize$closure);
    const record = { run: closure };
    return record;
  }
}

@export recursive[3] const recursivePeer = initialize {
  entry() {
    const closure = closure(recursivePeer$initialize$closure);
    const record = { run: closure };
    return record;
  }
}

closure recursiveValue$initialize$closure[](value) {
  entry() {
    if (value) {
      then();
    } else {
      else();
    }
  }
  then() {
    const literal = 3;
    if$join(literal);
  }
  else() {
    const recursivePeer = global(recursivePeer);
    const run = recursivePeer.run;
    const literal$1 = true;
    const call = source.call(run, literal$1);
    if$join(call);
  }
  if$join(result) {
    return result;
  }
}

closure recursivePeer$initialize$closure[](value) {
  entry() {
    if (value) {
      then();
    } else {
      else();
    }
  }
  then() {
    const literal = 4;
    if$join(literal);
  }
  else() {
    const recursiveValue = global(recursiveValue);
    const run = recursiveValue.run;
    const literal$1 = true;
    const call = source.call(run, literal$1);
    if$join(call);
  }
  if$join(result) {
    return result;
  }
}

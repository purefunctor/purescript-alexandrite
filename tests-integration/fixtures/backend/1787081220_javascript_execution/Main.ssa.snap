---
source: tests-integration/src/fixtures.rs
assertion_line: 256
expression: ssa_report
---
@export foreign const addInt;

@export foreign const decrementInt;

@export foreign const equalInt;

@export foreign const foreignValue;

@export foreign const effectValue;

@export foreign const await;

@export const integer = initialize {
  entry() {
    const literal = 42;
    return literal;
  }
}

@export const number = initialize {
  entry() {
    const literal = 1.5;
    return literal;
  }
}

@export const string = initialize {
  entry() {
    const literal = "alexandrite";
    return literal;
  }
}

@export const array = initialize {
  entry() {
    const literal = 1;
    const literal$1 = 2;
    const literal$2 = 3;
    const array = [literal, literal$1, literal$2];
    return array;
  }
}

@export const model = initialize {
  entry() {
    const literal = 0;
    const literal$1 = true;
    const record = { enabled: literal$1 };
    const await = global(await);
    const literal$2 = "data, not a prototype";
    const record$1 = {
      count: literal,
      nested: record,
      "hostile-field": await,
      __proto__: literal$2
    };
    return record$1;
  }
}

@export const updated = initialize {
  entry() {
    const model = global(model);
    const literal = 1;
    const literal$1 = false;
    const updated = record.update(model, { count: literal, nested: { enabled: literal$1 } });
    return updated;
  }
}

@export function readHostile(value) {
  entry() {
    const hostile_field = value["hostile-field"];
    return hostile_field;
  }
}

@export function readProto(value) {
  entry() {
    const __proto__ = value.__proto__;
    return __proto__;
  }
}

@export function capture(captured) {
  entry() {
    const closure = closure(capture$closure, captured);
    return closure;
  }
}

@export function apply(function, value) {
  entry() {
    const call = source.call(function, value);
    return call;
  }
}

@export function addCaptured(amount) {
  entry() {
    const closure = closure(addCaptured$closure, amount);
    return closure;
  }
}

@export const curried = initialize {
  entry() {
    const apply = global(apply);
    const addCaptured = global(addCaptured);
    const literal = 2;
    const call = source.call(addCaptured, literal);
    const call$1 = source.call(apply, call);
    const literal$1 = 40;
    const call$2 = source.call(call$1, literal$1);
    return call$2;
  }
}

@export function nestedJoin(outer, inner) {
  entry() {
    if (outer) {
      then();
    } else {
      else();
    }
  }
  then() {
    const foreignValue = global(foreignValue);
    if (inner) {
      then$1();
    } else {
      else$1();
    }
  }
  else() {
    const literal$2 = 0;
    if$join(literal$2);
  }
  if$join(result) {
    return result;
  }
  then$1() {
    const literal = 1;
    if$join$1(literal);
  }
  else$1() {
    const literal$1 = 2;
    if$join$1(literal$1);
  }
  if$join$1(result$1) {
    const addInt = global(addInt);
    const call = source.call(addInt, foreignValue);
    const call$1 = source.call(call, result$1);
    if$join(call$1);
  }
}

@export recursive[19] function countdown(value) {
  entry() {
    const equalInt = global(equalInt);
    const call = source.call(equalInt, value);
    const literal = 0;
    const call$1 = source.call(call, literal);
    if (call$1) {
      then();
    } else {
      else();
    }
  }
  then() {
    const literal$1 = 0;
    if$join(literal$1);
  }
  else() {
    const addInt = global(addInt);
    const literal$2 = 1;
    const call$2 = source.call(addInt, literal$2);
    const countdown = global(countdown);
    const decrementInt = global(decrementInt);
    const call$3 = source.call(decrementInt, value);
    const call$4 = source.call(countdown, call$3);
    const call$5 = source.call(call$2, call$4);
    if$join(call$5);
  }
  if$join(result) {
    return result;
  }
}

@export recursive[20] function isEven(value) {
  entry() {
    const equalInt = global(equalInt);
    const call = source.call(equalInt, value);
    const literal = 0;
    const call$1 = source.call(call, literal);
    if (call$1) {
      then();
    } else {
      else();
    }
  }
  then() {
    const literal$1 = true;
    if$join(literal$1);
  }
  else() {
    const isOdd = global(isOdd);
    const decrementInt = global(decrementInt);
    const call$2 = source.call(decrementInt, value);
    const call$3 = source.call(isOdd, call$2);
    if$join(call$3);
  }
  if$join(result) {
    return result;
  }
}

@export recursive[20] function isOdd(value) {
  entry() {
    const equalInt = global(equalInt);
    const call = source.call(equalInt, value);
    const literal = 0;
    const call$1 = source.call(call, literal);
    if (call$1) {
      then();
    } else {
      else();
    }
  }
  then() {
    const literal$1 = false;
    if$join(literal$1);
  }
  else() {
    const isEven = global(isEven);
    const decrementInt = global(decrementInt);
    const call$2 = source.call(decrementInt, value);
    const call$3 = source.call(isEven, call$2);
    if$join(call$3);
  }
  if$join(result) {
    return result;
  }
}

@export function capturedMutual(captured, condition) {
  entry() {
    const [localSecond, localFirst] = closures.recursive({
      localSecond: closure(localSecond$function, captured, localFirst),
      localFirst: closure(localFirst$function, captured, localSecond)
    });
    const call = source.call(localFirst, condition);
    return call;
  }
}

@export constructor None/0;

@export constructor Pair/2;

@export const pair = initialize {
  entry() {
    const Pair = constructor(Pair);
    const literal = 7;
    const call = source.call(Pair, literal);
    const literal$1 = 8;
    const call$1 = source.call(call, literal$1);
    return call$1;
  }
}

@export function first(choice) {
  entry() {
    case$0();
  }
  case$0() {
    const matches = pattern.constructor(choice, None);
    if (matches) {
      match$success();
    } else {
      case$1();
    }
  }
  case$1() {
    const matches$1 = pattern.constructor(choice, Pair);
    if (matches$1) {
      match$success$1();
    } else {
      case$failure();
    }
  }
  case$failure() {
    throw patternMatchFailure();
  }
  case$join(result) {
    return result;
  }
  match$success() {
    const literal = 0;
    case$join(literal);
  }
  match$success$1() {
    const left = pattern.argument(choice, Pair, 0);
    const argument = pattern.argument(choice, Pair, 1);
    case$join(left);
  }
}

@export function partialPattern($choice) {
  entry() {
    const matches = pattern.constructor($choice, Pair);
    if (matches) {
      match$success();
    } else {
      parameter$failure();
    }
  }
  parameter$failure() {
    throw patternMatchFailure();
  }
  match$success() {
    const left = pattern.argument($choice, Pair, 0);
    const argument = pattern.argument($choice, Pair, 1);
    return left;
  }
}

@export function unwrapWrapped($wrapped) {
  entry() {
    const matches = pattern.constructor($wrapped, Wrapped);
    if (matches) {
      match$success();
    } else {
      parameter$failure();
    }
  }
  parameter$failure() {
    throw patternMatchFailure();
  }
  match$success() {
    const value = pattern.argument($wrapped, Wrapped, 0);
    return value;
  }
}

@export const crossModule = initialize {
  entry() {
    const unwrapWrapped = global(unwrapWrapped);
    const wrapped = global(wrapped);
    const call = source.call(unwrapWrapped, wrapped);
    return call;
  }
}

@export const forwardReference = initialize {
  entry() {
    const forward = global(forward);
    return forward;
  }
}

@export instance const measureInt = initialize {
  entry() {
    const addInt = global(addInt);
    const literal = 1;
    const call = source.call(addInt, literal);
    const record = { measure: call };
    return record;
  }
}

@export const evidenceValue = initialize {
  entry() {
    const measureInt = global(measureInt);
    const measure = measureInt.measure;
    const literal = 41;
    const call = source.call(measure, literal);
    return call;
  }
}

closure capture$closure[captured]($int) {
  entry() {
    return captured;
  }
}

closure addCaptured$closure[amount](value) {
  entry() {
    const addInt = global(addInt);
    const call = source.call(addInt, amount);
    const call$1 = source.call(call, value);
    return call$1;
  }
}

closure localSecond$function[captured, localFirst]($boolean) {
  entry() {
    case$0();
  }
  case$0() {
    const matches = pattern.literal($boolean, true);
    if (matches) {
      match$success();
    } else {
      case$1();
    }
  }
  case$1() {
    const matches$1 = pattern.literal($boolean, false);
    if (matches$1) {
      match$success$1();
    } else {
      case$failure();
    }
  }
  case$failure() {
    throw patternMatchFailure();
  }
  case$join(result) {
    return result;
  }
  match$success() {
    case$join(captured);
  }
  match$success$1() {
    const literal = true;
    const call = source.call(localFirst, literal);
    case$join(call);
  }
}

closure localFirst$function[captured, localSecond]($boolean) {
  entry() {
    case$0();
  }
  case$0() {
    const matches = pattern.literal($boolean, true);
    if (matches) {
      match$success();
    } else {
      case$1();
    }
  }
  case$1() {
    const matches$1 = pattern.literal($boolean, false);
    if (matches$1) {
      match$success$1();
    } else {
      case$failure();
    }
  }
  case$failure() {
    throw patternMatchFailure();
  }
  case$join(result) {
    return result;
  }
  match$success() {
    case$join(captured);
  }
  match$success$1() {
    const literal = true;
    const call = source.call(localSecond, literal);
    case$join(call);
  }
}
